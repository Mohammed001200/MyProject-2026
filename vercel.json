{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "buildCommand": "DATABASE_URL=postgresql://build:build@localhost:5432/build corepack pnpm db:generate && corepack pnpm build",
  "crons": [
    {
      "path": "/api/internal/jobs/documents",
      "schedule": "0 4 * * *"
    }
  ]
}
